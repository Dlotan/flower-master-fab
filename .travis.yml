language: python

python:
  - "3.5"
  - "2.7"

install:
  - "pip install -r requirements.txt --use-mirrors"
  - "pip install coveralls"
  
script:
  - python manage.py test

after_success:
  coveralls